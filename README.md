### TODO:
- decide imports (time, pgtypes, etc), if an entity is used composite types, add imports to dtos/services/repos/etc.
- when a table has default values: decide which of them are included in create-update-requests (exclude system generated fields like: guid, created_at, etc..., include others)


