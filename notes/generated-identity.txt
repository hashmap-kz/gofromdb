create table orders
(
    id    serial primary key,
    price numeric,
    tax   numeric generated always as (price * 0.1) stored
);
insert into orders (price)
values (100);
-- works

-- error:  column "tax" is a generated column
update orders
set tax = 15
where id = 1;

----------------------------

create table users
(
    id   integer generated always as identity primary key,
    name text
);

insert into users (name)
values ('alice');

select *
from users;

-- error
update users
set id = 2
where id = 1;

